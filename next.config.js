/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['images.unsplash.com'],
    }
};

// Note: tempo-devtools swc plugin is not supported on Next.js 15+

module.exports = nextConfig;